/**
 * Agent Core (ТЗ §5, §9, §21, §22, §32, §33).
 *
 * Ядро не знает ни про React, ни про Windows и macOS: оно работает с реестром
 * инструментов и провайдерами. Это позволяет проверять инварианты ТЗ тестами,
 * не поднимая ни окно, ни операционную систему.
 */

export * from './agent/types'
export * from './permissions/gate'
