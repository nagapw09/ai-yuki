//! Реализация системного слоя для Windows 10/11 (ТЗ §2, §30).
//!
//! Крейт компилируется в пустоту на не-Windows целях, поэтому его можно безусловно
//! держать в зависимостях приложения — выбор адаптера делает вызывающая сторона.

#![cfg(windows)]

mod adapter;

pub use adapter::WindowsAdapter;
