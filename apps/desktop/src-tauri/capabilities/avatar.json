{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "avatar",
  "description": "Права окна аватара (ТЗ §12). Ему нужен только базовый IPC: состояние приходит событиями из главного окна, а модель — отдельной командой. Ни уведомлений, ни глобальных хоткеев здесь нет намеренно — окно рисует, а не действует.",
  "windows": [
    "avatar"
  ],
  "permissions": [
    "core:default"
  ]
}
